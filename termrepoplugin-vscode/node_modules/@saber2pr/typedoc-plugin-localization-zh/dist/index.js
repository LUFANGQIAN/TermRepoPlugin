"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.load = void 0;
const process = require("process");
const fs = require("fs-extra");
const convert_component_1 = require("./components/convert-component");
const render_component_1 = require("./components/render-component");
const constants_1 = require("./utils/constants");
const global_funcs_1 = require("./utils/global-funcs");
const template_strings_1 = require("./utils/template-strings");
const theme_component_1 = require("./components/theme-component");
const options_1 = require("./utils/options");
const path_1 = require("path");
__exportStar(require("./utils/helpers/localize"), exports);
function load(PluginHost) {
    const app = PluginHost.owner;
    /**
     * Add Options register Component.
     */
    (0, options_1.pluginOptions)(app.options);
    let startConverter = false;
    let startRenderer = false;
    const processArgs = process.argv;
    /**
     * Determines it it is necessary to run Conversion or Render process based on the
     * Command line arguments(Options).
     */
    processArgs.forEach(command => {
        if (command.indexOf(constants_1.Constants.CONVERT_OPTION) >= 0) {
            startConverter = true;
        }
        if (command.indexOf(constants_1.Constants.RENDER_OPTION) >= 0) {
            startRenderer = true;
        }
    });
    if (startConverter) {
        /**
         * We set the 'default' value of 'out' option because this option is checked before the converter has been started.
         * As we kill the process after the convertion of the json's we or not interested in the value of the 'out' option.
         */
        app.options.setValue('out', 'defult');
        /**
         * Register component responsible for the convertion.
         */
        new convert_component_1.ConvertComponent(app);
        // app.converter.('test', ConvertComponent(app));
    }
    if (startRenderer) {
        /**
         * Register component responsible for the Renderer.
         */
        new render_component_1.RenderComponenet(app);
    }
    /**
     * Register theme component.
     */
    new theme_component_1.ThemeComponent(app);
    // app.renderer.addComponent('theme-component', new ThemeComponent(app.renderer));
    registerHardcodedTemplateStrings(processArgs);
}
exports.load = load;
/**
 * Build the Cache containing all localized template strings.
 */
function registerHardcodedTemplateStrings(options) {
    const shellStringsFilePath = global_funcs_1.GlobalFuncs.getCmdLineArgumentValue(options, constants_1.Constants.TEMPLATE_STRINGS_OPTION);
    const local = global_funcs_1.GlobalFuncs.getCmdLineArgumentValue(options, constants_1.Constants.LOCALIZE_OPTION);
    if (!local) {
        return;
    }
    const templateStrings = fs.readJsonSync((0, path_1.join)(__dirname, './template-strings.json'));
    template_strings_1.HardcodedStrings.setLocal(local);
    template_strings_1.HardcodedStrings.setTemplateStrings(templateStrings);
}
