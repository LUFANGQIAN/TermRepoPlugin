"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Parser = void 0;
const util_1 = require("util");
class Parser {
    splitByCharacter(text, char) {
        const res = this.removeEmptyStrings(text.split(char));
        if (res.length === 1) {
            return res[0];
        }
        return res;
    }
    joinByCharacter(obj, char) {
        if (!(0, util_1.isArray)(obj)) {
            return obj;
        }
        return obj.join(char);
    }
    removeEmptyStrings(splittedText) {
        return splittedText.filter(el => el !== "");
    }
}
exports.Parser = Parser;
