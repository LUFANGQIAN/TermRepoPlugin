"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.GlobalFuncs = void 0;
class GlobalFuncs {
    static getCmdLineArgumentValue(options, key) {
        const indx = options.findIndex((e) => e === `--${key}`);
        if (indx >= options.length - 1 || indx < 0) {
            return;
        }
        return options[indx + 1];
    }
    static getKeyValuePairVal(data, key, value) {
        if (!!data && !!data[key]) {
            const res = data[key][value];
            return res ? res : value;
        }
        return value;
    }
}
exports.GlobalFuncs = GlobalFuncs;
